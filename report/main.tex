
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Visual Analytics of Bitcoin: A Multi-Scale Dashboard
% MSBD5005 (Spring 2026) — Team USTVIS14
%
% Built against the IEEE VGTC TVCG journal LaTeX template (2026-01-30
% release).  Compile with:    make            (uses the bundled makefile)
%                or:           latexmk -pdf main.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[journal,hideappendix]{vgtc}      % final (journal style), no appendix

\onlineid{0}
\vgtccategory{Research}

%% Paper title.
\title{Visual Analytics of Bitcoin:\\A Multi-Scale Dashboard for Market Regime
Discovery and Event Explanation}

%% Authors.  ORCID IDs left blank (insert via \authororcid{Name}{0000-...} when
%% available).
\author{%
  Zhuoang Cai,
  Jiaxin Huang,
  Wanlin Lu,
  Jialin Zhang, and
  Xiaotong Zhang
}

\authorfooter{
  \item All authors are with the Hong Kong University of Science and
    Technology, MSBD5005 (Spring 2026).
    E-mail (corresponding author): zcaiat@connect.ust.hk, jhuangfw@connect.ust.hk, wluav@connect.ust.hk, jzhangnm@connect.ust.hk, xzhangkg@connect.ust.hk.
}

%% Suppress the IEEE manuscript-received boilerplate at the bottom of
%% page 1 (course report, not an IEEE submission).
\renewcommand{\manuscriptnotetxt}{}

%% Abstract.
\abstract{%
A price chart shows that Bitcoin moved.  It cannot show when, what kind of day, or why.  We present a coordinated multi-scale dashboard that answers all three across one interaction
loop: a Macro long-range overview, a Meso regime-discovery view built on a UMAP projection of an eight-dimensional daily feature vector, and a Micro day-explanation view that pairs intraday price action with same-day GDELT headlines and Polymarket prediction-market odds.  The three views share a single client store, so a brush in one filters the others.  Beyond the chart inventory, the system makes a small but practical data-design contribution: per-window curated GDELT queries and Polymarket event slugs that turn two recency-biased public APIs into a date-aware historical context engine for four case-study windows (COVID 2020, War 2022, Election 2024, Iran 2026).  We validate the workflow on 2026-03-23, when Iran-related tensions coincided with a midday Bitcoin breakout, and trace the narrative end-to-end from macro anomaly to headline timing and prediction-market shifts. A free copy of this paper, all source code, and the cached data artifacts are available at
\url{https://github.com/CZA1006/BTC-Multi-Scale-Visualization}.%
}

%% Index terms (printed as ``Index Terms'' in journal mode).
\keywords{Coordinated multiple views, dimensionality reduction,
calendar heatmap, parallel coordinates, news visualization, financial
visual analytics, Bitcoin.}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%% LOAD PACKAGES %%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\graphicspath{{figs/}{figures/}{pictures/}{images/}{./}}


\usepackage{booktabs}
\usepackage{amssymb}                   % \blacktriangle / \blacktriangledown
\usepackage{mathptmx}                  % matching math font

\usepackage{hyperref} 

\usepackage{cleveref}



\begin{document}

\firstsection{Introduction}

\maketitle

Bitcoin (BTC) sits between an asset and a narrative.  Its price has
tripled and halved multiple times since 2019, with each regime change
tied to a different external story: a pandemic crash, a Federal
Reserve pivot, a sovereign war, two US presidential elections, and a
string of geopolitical shocks.  A line chart can show the magnitude
of those moves; it cannot tell the analyst when a regime
began, what kind of trading day sits inside that regime, or
why the market interpreted a given news bundle the way it did.
The three questions are not independent: each requires a different
scale of detail, and the analyst's mental model is built only
when all three are answered consistently for the same date.

This project builds a web-based visual analytics system over a
2{,}677-day BTC record (2019-01-01 to 2026-04-30) that answers all
three questions in a single interaction loop.  The system ingests four heterogeneous data layers, including BTC daily and hourly OHLCV from Yahoo Finance, three external assets (COIN, MSTR, QQQ) on the same calendar, $\sim 80$  thousand GDELT~\cite{gdelt} headlines covering the four case-study windows, and $\sim 6{,}700$ daily price points from 70 curated Polymarket~\cite{polymarket} prediction markets. It then projects each trading day into an eight-dimensional engineered feature vector, consisting of return, open-close change, high-low range, 30-day volume z-score, 7-day and 30-day rolling realised volatility, 30-day drawdown, and an aggregated GDELT news-tone field. This feature vector is the backbone of every cross-view computation in the system and the input to the UMAP embedding and KMeans clustering used in the Meso view.

The dashboard is organised around four analytical tasks distilled
from the project requirements:

\begin{itemize}
  \item[\textbf{T1}] \emph{Market evolution.}~How does BTC evolve over
    the seven-year horizon?
  \item[\textbf{T2}] \emph{Pattern discovery.}~Which trading days form
    typical regimes, and which are abnormal?
  \item[\textbf{T3}] \emph{Day-level explanation.}~For a chosen day,
    what happened hour by hour?
  \item[\textbf{T4}] \emph{Cross-market context.}~How do news flow,
    prediction markets, and external assets relate to BTC?
\end{itemize}

Each task is served by one of three coordinated views (Macro, Meso,
Micro) plus a context layer that overlays GDELT headlines, Polymarket
prediction-market odds, and three external assets (COIN, MSTR, QQQ).
The three views share a single client-side selection store: brushing a
Macro range filters the Meso scatterplot, clicking a Meso point sets the
Micro selected date, and selecting a date in Micro highlights it back in
Macro and Meso.  All four data layers feed the same daily-bucket index,
making cross-view linking a constant-time lookup.

The system was deliberately scoped narrow and deep: rather than
build a broad terminal-style toolkit, we focus on three high-bandwidth
views and a case-study navigator that walks the user through four
prepared windows.  The case study below focuses on 2026-03-23, a date within the Iran tension window, and exercises every link in the chain.

\section{Related Work}
\label{sec:related}

Our design rests on the foundational visualization literature.
Munzner's what/why/how triple~\cite{munzner2014},
Shneiderman's overview-first mantra~\cite{shneiderman1996}, Mackinlay's
channel rankings~\cite{mackinlay1986} and Cleveland and McGill's
perceptual experiments~\cite{cleveland1984} jointly justify our
encoding choices: position-on-common-scale for OHLC and volume,
color-on-diverging-scale only for centred quantities, and a
Macro$\to$Meso$\to$Micro flow that always shows the overview first.
Van Wijk and van Selow's calendar-cell layout~\cite{vanwijk1999} is the
canonical grid encoding we adopt for daily returns; Heer et
al.~\cite{heer2009} characterise the horizon-graph alternative we
considered but did not adopt once event markers were added.  For
multidimensional days we apply UMAP~\cite{mcinnes2018umap} together
with Inselberg's parallel coordinates~\cite{inselberg1985}, and Tufte's
small-multiples and data-ink principles~\cite{tufte2001} drive our
equity-curve sparklines.  The case-study navigator borrows Segel and
Heer's author-driven $\to$ reader-driven
spectrum~\cite{segel2010narrative}; all categorical encodings follow
Wong's color-blindness guidance~\cite{wong2011points} and ColorBrewer
discipline~\cite{colorbrewer}.

Most public crypto dashboards (Glassnode, CoinGecko, TradingView) treat
news as a sidebar decoupled from the chart.  GDELT~\cite{gdelt} and Polymarket~\cite{polymarket} are structurally biased toward today. Our date-aware historical extension to these two APIs is the practical contribution that distinguishes this work from a chart catalog.

\section{Design Tasks and Requirements}
\label{sec:design}

We follow Munzner's nested-model task abstraction~\cite{munzner2014} by mapping each domain question to an abstract task and then to a visualization idiom.  
T1 (\emph{market evolution}) summarizes temporal trends and identifies abnormal days in ordered time-series data, supported by linear and grid time-series layouts. 
T2 (\emph{pattern discovery}) compares multidimensional daily features and reveals recurring market states through dimension reduction, clustering, and parallel coordinates.  
T3 (\emph{day-level explanation}) inspects an aligned multivariate intraday slice using coordinated linear layouts on a common \(x\)-axis.
T4 (\emph{cross-market context}) looks up and compares heterogeneous evidence across external asset prices, news themes, and prediction-market probabilities in a coordinated contextual layout.

Six concrete requirements followed.
\textbf{R1, single-screen overview:} the Macro, Meso, and Micro views should fit on a 1440-px laptop screen without scrolling, which motivates compact encodings such as sparklines where appropriate. 
\textbf{R2, regime visibility:} the Meso view should make the cluster membership of every day in the brushed window legible at a glance through UMAP, cluster hulls, and parallel coordinates. 
\textbf{R3, date-aware context:} the news and prediction-market panels should reflect the selected date rather than the current API state, motivating the curated-bucket data design in \cref{sec:curated}. 
\textbf{R4, cross-scale consistency:} selections should propagate across all views within the same render cycle, allowing users to check a hypothesis across macro, meso, and micro scales without losing context. 
\textbf{R5, accessible encoding:} every diverging scale uses ColorBrewer RdYlGn-7~\cite{colorbrewer}; every categorical scale uses Set2 with BTC orange excluded; chip glyphs use redundant shape and hue encoding~\cite{wong2011points}.  . 
\textbf{R6, lightweight deployment:} the interface should remain small enough for responsive web use, with a target budget of \(\le 6\)~KB gzipped CSS and \(\le 130\)~KB gzipped initial JavaScript. 
The production build meets this budget with 5.00~KB CSS and 106.5~KB JavaScript.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



\section{System and Visualization Design}
\label{sec:system}

The system uses a FastAPI backend with three JSON endpoints:
\texttt{/api/overview}, \texttt{/api/meso}, and \texttt{/api/day-detail}.
The frontend is implemented in React 18 with Vite and Zustand for state management.
Visualizations are built with D3 v7 and ECharts v6; no chart library is used as a black box for our novel encodings.

%%%%%%%%%%%%%%%4.1
\subsection{Macro view (T1): Linear and grid layouts for time series}

The Macro view answers \emph{when}.  Following the standard
temporal-data taxonomy~\cite{munzner2014}, we combine two
complementary layouts: a \emph{linear} layout (the BTC daily timeline,
$x$ = time, $y$ = close price) and a \emph{grid} layout (the calendar
heatmap, one cell per trading day, color-encoding daily return).  We
deliberately avoided the \emph{circular} layout because BTC has no
strong intra-week or intra-month cyclicality at the daily granularity,
and we considered but did not adopt the horizon-graph
alternative~\cite{heer2009} once the event-marker layer was added: the
markers already supply the vertical-density payoff that horizon
graphs provide.

The line chart uses position on a common scale, Mackinlay's highest-ranked channel for quantitative data~\cite{mackinlay1986}. We overlay five canonical event markers, including FOMC pivots, war onsets, and ETF approvals, using shape together with hue to distinguish event categories. 
The calendar heatmap uses a ColorBrewer RdYlGn-7 diverging scale~\cite{colorbrewer} anchored at zero, with green indicating positive return, red indicating negative return, and yellow indicating near-zero return. To reduce reliance on color alone, a small dot in the corner of each cell encodes the GDELT event-density signal. Clicking a cell updates \texttt{selectedDate}, while brushing the timeline updates \texttt{selectedTimeRange} (\cref{fig:macro}). Both selections are propagated through the shared store to support coordinated brushing and linking across views.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%4.2 meso

\subsection{Meso view (T2): Multidimensional analysis}

The Meso view answers \emph{what kind of day}.  We combine dimension reduction and clustering to reveal recurring patterns in the eight-dimensional daily feature space. 
We project each daily feature vector to 2D using UMAP~\cite{mcinnes2018umap}, which is suitable for capturing nonlinear neighborhood structure in exploratory analysis. We then cluster the original feature vectors with KMeans and overlay \(k=5\) convex hulls on the projection.
Spatial proximity in the scatter encodes market similarity: nearby points are similar trading days; cluster color encodes the regime label.  
We use the ColorBrewer Set2 categorical palette and exclude orange to avoid ambiguity with the primary BTC encoding, following the principle that color should primarily support labeling rather than decoration~\cite{tufte2001}.



A regime summary band below the scatter makes the cluster story
quantitative through three coordinated panels.  (1)~A \emph{cluster
summary table} reports $n$, mean daily return, annualized volatility,
and a 60-point equity-curve \emph{sparkline} for each
cluster~\cite{tufte2001} --- the small-multiples idiom so the reader
compares regimes by \emph{shape}, not by scrolling between
numbers.~(2)~A toggleable \emph{parallel-coordinates}
plot~\cite{inselberg1985} over the seven engineered features restores
the axes UMAP collapsed; the selected day is drawn as a thick line on
top of a faded cluster average, supporting per-day-vs-regime
comparison directly.~(3)~A $4{\times}4$ cross-asset \emph{correlation
matrix} (Pearson over the active window) over BTC, COIN, MSTR, and QQQ
uses a green$\leftrightarrow$red diverging scale anchored at zero.
Selecting a cluster in the legend filters all three panels in one
render (\cref{fig:meso}).

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%4.3
\subsection{Micro view (T3, T4) --- intraday detail and context layer}

The Micro view answers \emph{what happened} on a selected day and
\emph{how} cross-market signals relate to it.  Five coordinated
elements compose it.~(1)~\emph{Summary cards} report selected-day
open/high/low/close, intraday range, and 24-hour change; the up/down
chip uses redundant shape (\,$\blacktriangle/\blacktriangledown$\,) and
hue, satisfying the redundancy guideline for color-blind
accessibility~\cite{wong2011points}.~(2)~The \emph{intraday chart}
follows the time-series linear-layout idiom: two stacked
grids (candlesticks above, volume bars below) sharing one $x$-axis,
so \emph{position-on-common-scale} carries the alignment between price
and traded volume (Mackinlay's top-ranked
channel~\cite{mackinlay1986}; \cref{fig:micro-intraday}).~(3)~The
\emph{external-asset context strip} renders three sparklines (COIN,
MSTR, QQQ) over the same window --- small multiples answering ``did
the rest of the risk-asset complex move with BTC?''
~(4)~The~\emph{headline panel} (T4) lists GDELT articles for the
selected day, sorted by tone and tagged with one of seven theme
buckets (war, election, covid, regulation, macro, crypto, other) ---
text-visualization in the categorical-tagging mode rather than the
word-cloud mode, to keep the reader's attention on which
\emph{narrative} dominates a given hour rather than on individual
keywords.  The active GDELT \emph{bucket} (curated case-study window)
appears as a provenance line above the panel.  (5)~The
\emph{Polymarket panel} (T4) renders per-curated-market cards, each
showing a daily YES-probability sparkline with a marker at the
selected date, the YES price observed at that date, and the current
resolution price (\cref{fig:micro-context}).


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%4.4
\subsection{Date-aware historical context: the data-design contribution}
\label{sec:curated}

The two public APIs we lean on for T4 are recency-biased.  GDELT's DOC
API returned bitcoin-only headlines for any query that matched
\texttt{bitcoin OR crypto}, leaving COVID-2020 or election-2024 days
with near-empty headline panels.  Polymarket's keyword search returns
\emph{today's} active markets, useless for a query asking ``what did the
crowd believe on 2024-11-05.''

We solve both with the same \emph{curated-bucket} pattern.  Each of the
four case-study windows (COVID, War, Election, Iran) owns:

\begin{itemize}
  \item A tailored GDELT DOC query (e.g., the 2022 War bucket queries
    \texttt{(ukraine OR russia OR putin OR sanctions OR swift OR \ldots)})
    that keeps a \texttt{bitcoin OR crypto} tail clause so the asset
    chart is always anchored, plus a six-category theme map (war,
    election, covid, regulation, macro, crypto) so non-crypto windows
    still produce a populated theme-tagged headline panel.
  \item A short list of curated Polymarket event slugs (e.g.,
    \texttt{presidential-election-winner-2024},
    \texttt{us-iran-nuclear-deal-by-april-30}).  For each slug we fetch
    the full Gamma event payload and the per-market CLOB
    \texttt{/prices-history} daily series, then filter to markets active
    on the selected date with a 14-day post-resolution grace window so
    a market that resolved the day before still surfaces with its
    closing probability.  A \$1k volume floor and top-8 cap drop the
    long tail of zero-volume markets that would otherwise swamp the
    panel.
\end{itemize}

A bulk-warm script seeds 287 cached headline payloads and 6{,}708
Polymarket price points across the four windows.  An auto-rollup step
re-emits \texttt{gdelt\_daily\_signals.csv} after every fetch so the
Macro heatmap dot indicator stays in sync.  This is the piece that
turns two ``today-only'' APIs into a date-aware historical context
engine; it is implemented in roughly 300 lines of Python and is the
contribution we are proudest of beyond the chart inventory.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%4.5
\subsection{Implementation}
\label{sec:impl}

The backend is a FastAPI service of $\sim$1{,}000 lines of Python
exposing three JSON endpoints.  The frontend is a Vite + React 18
single-page app of $\sim$3{,}500 lines of JavaScript and JSX, with
selection state held in a Zustand store of seven keys
(\texttt{selectedTimeRange}, \texttt{selectedDate},
\texttt{selectedCluster}, \ldots) that every view subscribes to.  All
custom encodings (calendar heatmap, UMAP scatter with hulls, parallel
coordinates, correlation matrix, Polymarket sparkline-with-marker)
are written directly in D3~v7 and SVG; the intraday OHLC and volume
chart is the only ECharts dependency, code-split out of the main
bundle so it only loads when the Micro view becomes visible.  The
production build measures 5.00~KB of CSS and 106.5~KB of JavaScript
gzipped, comfortably inside the bundle budget defined in
\cref{sec:design}.



%%%%%%%%%%%%%% Figure placeholders (replace once screenshots are in figures/) %%%%%%%%%%%%%%

%%%%%%%%%%%%%macro
\begin{figure}[t]
  \centering
  % \fbox{\rule{0pt}{40mm}\rule{0.95\linewidth}{0pt}}
  \includegraphics[width=\linewidth]{macro}
  \caption{\textbf{Macro view (T1)} brushed to the 2026 Iran-tension
  window.  Calendar-heatmap cells encode daily returns on a CB-safe
  diverging RdYlGn-7 scale anchored at zero; the deepest cell on
  2026-03-23 is the visual question that drives the rest of the case
  study.  \emph{Figure placeholder; screenshot to be supplied by the
  team.}}
  \label{fig:macro}
\end{figure}


%%%%%%%%%%%%%%%meso
\begin{figure}[t]
  \centering
  % \fbox{\rule{0pt}{40mm}\rule{0.95\linewidth}{0pt}}
  \includegraphics[width=\linewidth]{meso}
  \caption{\textbf{Meso view (T2).}  UMAP projection of the
  eight-dimensional daily feature vector colored by KMeans cluster, with
  convex hulls and the selected day (2026-03-23) marked.  The regime
  summary table (right) and the parallel-coordinates plot (below, not
  shown) make the cluster characterization quantitative.
  \emph{Placeholder.}}
  \label{fig:meso}
\end{figure}

\begin{figure}[t]
  \centering
  \fbox{\rule{0pt}{40mm}\rule{0.95\linewidth}{0pt}}
  % \includegraphics[width=\linewidth]{micro-intraday}
  \caption{\textbf{Micro view: intraday OHLC and volume (T3)} for
  2026-03-23.  The flat morning, midday breakout, and sustained
  afternoon rally are visible in three contiguous regimes; the volume
  bars confirm the breakout is not a thin-tape artifact.
  \emph{Placeholder.}}
  \label{fig:micro-intraday}
\end{figure}

\begin{figure}[t]
  \centering
  \fbox{\rule{0pt}{40mm}\rule{0.95\linewidth}{0pt}}
  % \includegraphics[width=\linewidth]{micro-context}
  \caption{\textbf{Micro view: headline panel and Polymarket panel
  (T4).}  Morning headlines lean negative and geopolitical; midday a
  concentration of mainstream price-breakout reports lands; afternoon
  shifts to mixed political/regulatory.  Polymarket cards show
  YES-probability sparklines with a marker at the selected date.
  \emph{Placeholder.}}
  \label{fig:micro-context}
\end{figure}

\section{Case Study: 2026-03-23, Iran Tension}
\label{sec:case}

We validate the workflow on a single deep case study --- 2026-03-23,
inside the curated Iran-tension window.  The user begins from the
case-study navigator with no prior knowledge of the date.

\textbf{T1 --- locate the anomaly.}  The user clicks the
\emph{Iran Tension} chip.  The Macro window snaps to
2026-03-01\,\(\to\)\,2026-04-30.  In the calendar heatmap
(\cref{fig:macro}), 2026-03-23 reads as a deep green cell --- a
strong-positive day in an otherwise mixed window.  Hovering shows the
daily return; clicking sets \texttt{selectedDate}, which propagates to
Meso and Micro in the same render.

\textbf{T2 --- characterize the regime.}  In the Meso view
(\cref{fig:meso}), the selected day lands inside the
\emph{Bullish-Momentum} cluster --- the same cluster that contains
other midday-breakout days from the 2024 election cycle.  The
parallel-coordinates overlay shows that 2026-03-23's profile sits
above the cluster mean on \texttt{rolling\_volatility\_7d} and well
below on \texttt{drawdown\_from\_30d\_high} --- the day is volatile
but \emph{not} correcting from a peak, which is consistent with a
momentum breakout rather than a relief rally.  The cluster summary
table confirms this regime has the steepest equity-curve sparkline of
the four populated clusters.

\textbf{T3 --- drill into the day.}  In the Micro view's intraday chart
(\cref{fig:micro-intraday}), 2026-03-23 splits cleanly into three
regimes: a flat-to-weak morning UTC session, a sharp midday breakout
(the single bar in which BTC clears the prior day's high on visibly
elevated volume), and a sustained afternoon rally.  The summary cards
report the day's intraday range and 24-hour change with a green
$\blacktriangle$ chip.

\textbf{T4 --- find supporting evidence.}  The external-asset strip
shows COIN and MSTR both rallying in step with BTC --- the day is a
risk-on move, not a crypto-isolated event.  The headline panel
(\cref{fig:micro-context}) tells the chronology that the price chart
hints at: \emph{morning} headlines are negative and geopolitical
(Iran-deal talks stalling, regional tension); around the breakout, a
concentration of mainstream financial outlets file ``BTC breaks above
\$X'' reports --- the market moves first and the press reports
second; \emph{afternoon} headlines shift to mixed political and
regulatory commentary.  The curated Polymarket cards show small but
visible YES-probability shifts on the
\texttt{us-iran-nuclear-deal-by-april-30} and
\texttt{will-trump-declare-war-on-iran-by} markets, with the marker on
the sparkline placing 2026-03-23 squarely on the inflection.

\textbf{What we learn.}  No single panel proves causality; the
\emph{flow} provides the evidence.  Macro localises the anomaly, Meso
shows that anomalies of this type cluster with prior breakouts, Micro
shows the intraday regime change, and the headline+Polymarket layer
supplies a coherent narrative for \emph{why} the regime change
happened when it did.  Critically, three of the four panels would
have been \emph{unable to surface this story} before our P8/P9 data
work: the headline panel would have shown bitcoin-only news, the
Polymarket cards would have shown today's markets, and the Meso
cluster would have lacked the historical comparators that put a 2026
breakout in the same neighbourhood as 2024 election-night and 2020
post-halving days.  The system's contribution is the coherence of the
loop, not any one chart.

\textbf{A finding visible only at multi-scale.}~Because the headline
panel is timestamped at 15-minute granularity and the intraday chart
shares the same UTC $x$-axis, the user can see directly that on
2026-03-23 the breakout candle prints \emph{before} the mainstream
financial press files its breakout coverage --- the cluster of
``BTC breaks above \$X'' headlines arrives several minutes after the
volume spike, not before it.  This kind of micro-scale lead/lag
between price and news flow is invisible in a single-line price chart
and is also invisible in a single-day news feed; it is only legible
because the two views share both a $y$-axis-aligned data backbone
(daily UTC) and a coordinated selection.  We treat this as the
methodological finding of the case study, not the price move itself.

\section{Discussion}
\label{sec:discussion}

\subsection{Visualization principles applied}
\label{sec:knowledge}

\Cref{tab:concept-map} summarises the visualization concepts the
project applies and where each one shows up in the system.  Every
chart in \cref{sec:system} has a single-line justification in this
canon; the table is intentionally exhaustive so the relationship
between principles and built artefacts is auditable.

\begin{table}[t]
\centering
\caption{Visualization concepts applied in this system, with the
concrete design decision each one drives.}
\label{tab:concept-map}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}p{0.30\linewidth} p{0.62\linewidth}@{}}
\toprule
\textbf{Concept} & \textbf{Where applied in our system} \\
\midrule
Channel rankings; pre-attentive perception            & Position-on-common-scale for OHLC and volume; redundant shape+hue $\blacktriangle/\blacktriangledown$ chips. \\
Diverging vs.\ categorical color encodings; Tufte's ``do no harm''     & RdYlGn-7 diverging for centred quantities (returns, correlation); Set2 categorical for clusters with BTC orange excluded. \\
Semantic meaning over identity                        & Cluster labels (``Risk-Off,'' ``Bullish-Momentum'') replace numeric IDs; every chart is justified by data type and task before its idiom is chosen. \\
Munzner's nested-model task abstraction               & T1--T4 stated as abstract tasks (\texttt{summarise+identify}, \texttt{cluster+compare}, \texttt{identify+inspect}, \texttt{lookup+compare}) before idiom selection. \\
Time-series layouts: linear / grid / circular / timeline & Linear layout for Macro line and Micro intraday OHLC; grid layout for Macro calendar heatmap; circular and horizon-graph alternatives considered and rejected with reasons. \\
Dimension reduction; clustering; parallel coordinates & UMAP projection of the 8-D daily feature vector; KMeans $k=5$; parallel coordinates over 7 features; bidirectional brushing-and-linking across all three views. \\
Text visualization: theme tagging                     & 7-bucket theme map (war / election / covid / regulation / macro / crypto / other) over GDELT articles, each tagged with theme and tone. \\
Heuristic evaluation; controlled study design         & Internal heuristic evaluation against Munzner's triple recorded in \texttt{HEURISTIC\_EVALUATION.md}; a between-subject study protocol is drafted in \texttt{USER\_STUDY\_PROTOCOL.md} but deferred. \\
Narrative visualization: author-driven $\to$ reader-driven & Case-study navigator: four prepared windows (COVID, War, Election, Iran) give an author-driven entry; free brushing takes over once inside a window. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Sophistication and findings}

The chart inventory is intentionally mainstream; the project's design
ambition is the \emph{coordinated flow}, not novel chart shapes.  The
genuinely novel piece is the data-design contribution in
\cref{sec:curated}: per-window curated GDELT queries plus curated
Polymarket event slugs convert two recency-biased public APIs into a
date-aware historical context engine, which is what makes the
case-study workflow work at all on 2020 / 2022 / 2024 dates.  The
2026-03-23 case study (\cref{sec:case}) is the empirical demonstration
that all four scales line up: a midday breakout visible in Macro
($+$3.4\,\% return cell) clusters with prior-cycle breakout days in
Meso, splits cleanly into three intraday regimes in Micro, and is
anticipated by a discrete shift in the curated Iran-tension Polymarket
markets in T4.  The finding we want to highlight is methodological:
\emph{news flow lags price even on highly news-driven days} --- the
midday-breakout candle prints before the mainstream-financial press
files its breakout reports, a pattern only visible because all three
scales share one $x$-axis-aligned selection.

\subsection{Limitations}

The GDELT cache is currently $\sim$60\,\% complete across the COVID,
War, and Election windows (the Iran window is fully covered).  GDELT's
DOC API rate-limits at one request per five seconds, so filling the
gap is an incremental top-up rather than a code change.  Polymarket
coverage is by design limited to the 2024 Election and 2026 Iran
windows; the 2020 and 2022 windows return a friendly ``no Polymarket
coverage for this period'' message rather than empty cards.  A
light-theme palette and a side-by-side cross-window comparison view
are deferred to future work.

\section{Conclusion}
\label{sec:conclusion}

A multi-scale BTC dashboard is not, by itself, an interesting research
artifact.  What we hope is interesting is the demonstration that a
small visualization system can answer \emph{when}, \emph{what kind of
day}, and \emph{why} in one interaction loop --- provided the news
and prediction-market panels are made date-aware rather than
today-only.  The case study traces the 2026-03-23 Iran-tension
breakout from the deepest heatmap cell to the midday volume spike to
the headline-and-Polymarket narrative that explains the regime
change, with no panel switch and no manual cross-referencing.

Three takeaways close the work.  (i) Recurring market regimes are
visible once daily features are projected and clustered, where a
price line alone would obscure them.  (ii) A single-channel view is
insufficient: return, volatility, drawdown, and volume must be
co-displayed for anomalies to surface.  (iii) External signals (news,
prediction markets) are best read as \emph{supporting evidence}, not
direct proof of causality; the dashboard is designed to make that
distinction visible to the user rather than hide it.


\acknowledgments{%
  We thank the MSBD5005 (Spring 2026) teaching team for design feedback
  during studio sessions, and the maintainers of GDELT, Polymarket, and
  yfinance for keeping their APIs publicly accessible.%
}

\bibliographystyle{abbrv-doi-hyperref}
\bibliography{references}

\end{document}
